const initState = {
    data: {}
}

function reducer(state = initState, action) {
    switch (action.type) {
        default:
            return state
    }
    return state
}

module.exports = reducer
